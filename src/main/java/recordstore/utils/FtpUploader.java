package recordstore.utils;

//import org.springframework.integration.ftp.session.DefaultFtpSessionFactory;
//import org.springframework.integration.ftp.session.FtpSession;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.io.InputStream;

public class FtpUploader {

//    public static void uploadToFTP(String filename, MultipartFile multipartFile, DefaultFtpSessionFactory factory) {
//        try (FtpSession session = factory.getSession();
//             InputStream inputStream = multipartFile.getInputStream()) {
//            session.write(inputStream, filename);
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
//    }
}